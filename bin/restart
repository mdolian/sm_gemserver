#!/bin/sh

eyapi get \
  string "gemserver/user" default "gemserver"\

if user is not ${gemserver_user}
then
  log error "sm gemserver stop must be run as ${gemserver_user}"
fi

exec sm gemserver stop
exec sm gemserver start
